# Merged PRs: torvalds/linux

No merged pull requests found. The GraphQL query for the 5 most recently updated
merged PRs (`states: [MERGED]`, ordered by `UPDATED_AT`) returned an empty result set.

Follow-up check with `gh api repos/torvalds/linux` confirmed the repository exists
and is accessible (not renamed or moved), so the empty result is genuine: this
repository has no pull requests merged through GitHub. The Linux kernel uses a
mailing-list workflow (patches via email, applied by maintainers); pull requests
opened against this mirror on GitHub are closed rather than merged.

Collected: 0 merged PRs.
