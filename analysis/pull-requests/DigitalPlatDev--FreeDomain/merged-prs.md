# Merged PRs: DigitalPlatDev/FreeDomain

No merged pull requests were found for this repository.

- The GraphQL query for merged PRs (`states: [MERGED], last: 5`) returned zero results.
- Follow-up checks confirmed the repository exists and was not renamed or moved:
  - `gh api repos/DigitalPlatDev/FreeDomain` returned `full_name: DigitalPlatDev/FreeDomain` (default branch `main`, not a fork).
  - `pullRequests(states: [MERGED]) { totalCount }` reported **0**, and total pull requests of any state is also **0** — this repository has never had a pull request.
- Collected on: 2026-08-30.
