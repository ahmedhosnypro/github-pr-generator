# Merged PRs: massgravel/Microsoft-Activation-Scripts

No merged pull requests found.

- The repository exists and is reachable (`gh api repos/massgravel/Microsoft-Activation-Scripts` succeeded; default branch `master`).
- The GraphQL query for `pullRequests(states: [MERGED], last: 5, orderBy: {field: UPDATED_AT, direction: ASC})` returned an empty list.
- A follow-up count query confirmed `pullRequests(states: [MERGED]) { totalCount }` = **0**.

This project appears to accept contributions through channels other than merged GitHub PRs (issues/discussions), so there is no merged-PR data to collect.
