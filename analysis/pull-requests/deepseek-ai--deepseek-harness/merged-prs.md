# Merged PRs: deepseek-ai/deepseek-harness

No merged pull requests were found.

The GraphQL query for merged PRs (`states: [MERGED], last: 5, orderBy: UPDATED_AT ASC`) succeeded but returned an empty list. A follow-up check via `gh api repos/deepseek-ai/deepseek-harness` confirms the repository exists and is accessible (full name unchanged, default branch `master`, created 2026-08-13), so the repo simply has zero merged pull requests as of the collection date.
